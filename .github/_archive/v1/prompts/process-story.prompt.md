---
mode: agent
agent: coordinator
description: "Process a story through the full DevAgent pipeline"
---

Process the following task through the complete DevAgent workflow
(requirements → codebase analysis → planning → development → testing → review → git):

${input:task_description:Paste Jira link OR describe the task}
